package hospital;

public interface MedicalDuties {
	// code will never be written in interface. It's just method without
	// implementation. Are waiting to be used in other areas.

	void drawBlood(Patient patient);

	void administerCare(Patient patient);

}